# ENOB > 7.5 bits Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Optimize existing schematics in 8-bit Asynchronous SAR ADC on GF180MCU 3.3V to achieve new PRD performance targets: ENOB > 7.5 bits, SNDR > 47.0 dB, DNL < 1.0 LSB, INL < 1.0 LSB, with 100% clean netlists and 0 DRC/LVS violations.

**Architecture:** Successive Approximation Register ADC using charge-scaling CDAC (512 fF total), high-transconductance StrongARM dynamic comparator, and asynchronous domino handshake logic.

**Tech Stack:** GlobalFoundries GF180MCU 3.3V PDK, Xschem, Ngspice, KLayout, Netgen.

**Spec:** [`docs/PRD_8bit_Async_SAR_ADC_GF180MCU_3V3.md`](file:///home/kyrie/eda/designs/chipathon26-SAR_ADC/docs/PRD_8bit_Async_SAR_ADC_GF180MCU_3V3.md)

## Global Constraints
- Target Technology: GlobalFoundries GF180MCU (180 nm CMOS, open-source PDK)
- Target Supply Voltage: 3.3 V single supply
- ENOB: > 7.5 bit
- SNDR: > 47.0 dB
- INL / DNL: < 1.0 LSB
- Power: < 1.0 mW
- Sampling Rate: 20 - 50 MS/s
- Only modify existing schematics (no new schematics)
- Top-level netlist must be 100% clean (0 errors, 0 warnings, 0 floating nets)

---

### Task 1: Optimize StrongARM Comparator Sizing
**Files:**
- Modify: `xschem/sar_adc/blocks/comparator/strongarm_comp_core.sch`

- [ ] **Step 1: Size Mtail and input pair M1, M2 for wide dynamic common-mode**
  - $W(M_{tail}) = 16.00\,\mu\text{m}$, $L=0.28\,\mu\text{m}$
  - $W(M_1, M_2) = 8.40\,\mu\text{m}$, $L=0.28\,\mu\text{m}$
  - $W(M_3, M_4) = 2.40\,\mu\text{m}$, $L=0.28\,\mu\text{m}$
  - $W(M_5, M_6) = 3.60\,\mu\text{m}$, $L=0.28\,\mu\text{m}$
  - $W(M_7 - M_{10}) = 1.20\,\mu\text{m}$, $L=0.28\,\mu\text{m}$

- [ ] **Step 2: Verify schematic integrity**
  - Run `python3 scripts/check_all_schematics.py`
  - Expected: 34/34 OK, 0 Errors, 0 Warnings

---

### Task 2: Netlist and Verify Static Linearity
**Files:**
- Test: `scripts/characterize_static_suite.py`

- [ ] **Step 1: Generate clean netlist**
  - Netlist `tb_sar_adc_top.sch`
  - Run `python3 scripts/characterize_static_suite.py`
  - Expected: Max DNL < 1.0 LSB, Max INL < 1.0 LSB

---

### Task 3: Dynamic Spectral Characterization (ENOB & SNDR)
**Files:**
- Modify: `scripts/characterize_dynamic_suite.py`

- [ ] **Step 1: Execute 64-point FFT dynamic test at 1.09 MHz and 4.53 MHz**
  - Run `python3 scripts/characterize_dynamic_suite.py`
  - Expected: ENOB > 7.5 bits, SNDR > 47.0 dB

---

### Task 4: Layout Physical Verification (DRC & LVS)
**Files:**
- Test: `scripts/verify_all.py`

- [ ] **Step 1: Run DRC and LVS across all 14 cells**
  - Run `python3 scripts/verify_all.py`
  - Expected: 14/14 PASS (0 DRC violations, 100% LVS match)
